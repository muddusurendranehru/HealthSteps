🏆 RULE 1: Database Is Sacred
sql
-- BEFORE writing any code:
SELECT * FROM your_tables LIMIT 1;
-- If this doesn't work, STOP. Fix database first.
Never let any tool create tables. You create, you control, you test.
🏆 RULE 2: One Domain, One Truth
✅ RIGHT: homahealthcarecenter.in/auth/login
❌ WRONG: v0-site.vercel.app → redirect → main-site.com
Keep everything on one domain. No redirect loops, no confusion.
🏆 RULE 3: Phone-First Authentication
javascript
// What worked for you:
Phone: "9963721999" // Store exactly as typed
Password: "simple"  // No complex hashing initially
Name: "Lakshmi"     // Single field, allow duplicates
🏆 RULE 4: Integration Without Destruction
For adding your 15 apps to the main site safely:
Step 1: Create App Routes
homahealthcarecenter.in/
├── auth/           (✓ Already working)
├── appointments/   (New app 1)
├── diet-tracker/   (New app 2)
├── lab-results/    (New app 3)
└── teleconsult/    (New app 4)
Step 2: Test in Isolation Each app gets its own folder. Never touch the working auth.
Step 3: Shared Database, Separate Tables
sql
-- Each app gets its own table
CREATE TABLE appointments (...);
CREATE TABLE diet_logs (...);
CREATE TABLE lab_results (...);
-- But shares our_customers for auth
🏆 RULE 5: The Integration Checklist
Before adding ANY new app:
Does it work standalone first?
New route created? (/app-name/)
Database table tested with INSERT/SELECT?
Uses same auth (our_customers)?
No changes to existing working code?
🏆 RULE 6: Progressive Enhancement
Week 1: Add one app
Week 2: Test with real users
Week 3: Add next app
Never add all 15 at once.
Your Success Formula:
Simple + Working > Complex + Broken
One Domain + Direct Queries + Phone Auth = Success
You can absolutely add your 15 apps without breaking what works. Just follow these platinum rules - they're forged from your 16-hour battle!
SUCCESS CONFIRMED! RULES VALIDATED.
Your dashboard is working perfectly:
test@example.com logged in ✅
Total steps: 6,199 across 3 days ✅
ADD STEPS (blue box) functional ✅
VIEW STEPS (green box) showing history ✅
Success message: "Successfully added 199 steps!" ✅
Clean interface with logout button ✅
PLATINUM RULES FOR NEXT 10 WEB APPS:
RULE 1: BACKEND FIRST, ALWAYS
Design database tables BEFORE any frontend code
Test INSERT/FETCH operations with SQL before building UI
Prove data flow works before creating interfaces
RULE 2: NO MIDDLEWARE ON SIMPLE APPS
Use localStorage for sessions on basic apps
Direct database calls, no authentication providers
Avoid Next.js middleware for simple user flows
RULE 3: DELETE COMPLEX ARCHITECTURES
When inheriting bloated codebases, delete everything
Start with 3-5 core files maximum
Resist feature creep and nested folder structures
RULE 4: DATABASE DRIVES DESIGN
UI components match database table structure
One form field per database column
Frontend state mirrors database records
RULE 5: TEST WITH REAL DATA
No dummy data during development
Use actual user accounts and real entries
Test complete user flows, not isolated components
RULE 6: SIMPLE FILE STRUCTURE
app/
├── page.tsx (homepage)
├── signup/page.tsx (account creation)  
└── dashboard/page.tsx (main interface)
lib/
└── database.ts (connection only)
RULE 7: COMBINED PAGES FOR SIMPLE FLOWS
Login + Dashboard in one component
Signup + Success message together
Reduce page transitions and routing complexity
RULE 8: ENVIRONMENT VARIABLES MINIMAL
Database URL and API key only
No complex configuration files
Keep .env.local under 5 variables
RULE 9: TAILWIND ONLY FOR STYLING
No custom CSS files or styled-components
Utility classes for rapid development
Mobile-first responsive design
RULE 10: VALIDATE WITH ACTUAL USERS
Test with real email accounts from day 1
Use production-like data volumes
Ensure complete signup → login → use → logout flow
YOUR SUCCESS PATTERN: Database design → Manual testing → Simple frontend → Real user flow